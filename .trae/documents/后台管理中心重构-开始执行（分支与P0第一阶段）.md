## 我已阅读的开发计划（来源）
- 已阅读 [后台管理中心业务重构-需求文档与Backlog.md](file:///home/zl/work/workspaces/phowson/.trae/documents/后台管理中心业务重构-需求文档与Backlog.md)，将严格按“第1阶段（P0）：先 A+B，再 C→D→E→F，最后 I/J/K/G”的顺序推进。

## 执行前置（必须先做）
### 1) 创建开发分支
- 从当前工作分支新开：`refactor/admin-center`（如你更喜欢命名：`feat/admin-refactor` 也可）。
- 后续所有改动都落在该分支，避免影响主线。

## 第 1 阶段（P0）本轮要交付的内容（对应 Epic A+B）
### Epic A：后台整体布局与导航（P0）
- A1：把当前 `/admin` 单页 Tab，迁移为“/admin 下的嵌套路由 + 统一 Layout”。
  - 产物：`AdminLayout`（左侧导航 + 右侧内容区）
  - 目标：不破坏现有功能，先完成骨架与导航可用。
- A2：左侧导航分组
  - 顶部固定“上传新照片”按钮（跳转现有 `/upload`）
  - 下方菜单分两块：个人区 / 管理员区，并做明显视觉分隔
- A4：路由守卫
  - 未登录：跳转登录
  - 非管理员：不可进入 `/admin/manage/*`（菜单隐藏 + 路由层拦截双保险）

### Epic B：通用组件与规范（P0）
- B1：统一分页组件
  - 能力：页码、每页条数、跳转、总数/范围展示、键盘可操作
  - 先在“列表类页面（管理员照片/评论/用户）”接入一处做基线
- B3：统一空/加载/错误态
  - 产物：可复用的 `EmptyState`/`LoadingState`/`ErrorState` 组件（或一个组合组件）
- B4：统一 toast 与确认对话框
  - 复用现有 Toast/Modal（若已存在）或在项目现有范式上补齐
  - 规则：成功/失败用 toast；高风险操作用确认对话框

## 渐进式迁移策略（避免一次性大爆炸）
- 第一步只做“路由与 Layout 重构”，内容页先用“薄封装”：
  - `/admin/manage/photos` 先复用当前 Admin 的照片管理逻辑
  - 其余路由先占位（显示“建设中”+ 统一空状态），保证导航结构稳定
- 等 A+B 稳定后，再进入个人域 C→D→E→F 的真实业务开发。

## 验证与测试（每个 P0 交付都要可验证）
- 前端手动验收清单（P0）：
  - `/admin` 打开后：左侧菜单分组与上传按钮可见
  - 点击“上传新照片”能正确跳转 `/upload`
  - 个人区路由可进入；非 admin 无法进入 manage 路由
  - 移动端：侧边栏抽屉可打开/关闭，遮罩可关闭
- 自动化测试策略（在落地实现时补齐）：
  - 路由守卫：非 admin 访问 `/admin/manage/*` 返回被拦截（前端单测或轻量 e2e）
  - 分页组件：边界（第一页/最后一页/更改每页条数/跳转非法页码）

## 本轮改动范围（预计会涉及的文件/目录）
- 路由入口：[App.tsx](file:///home/zl/work/workspaces/phowson/App.tsx)（新增嵌套路由与布局挂载点）
- 后台页面：[pages/Admin.tsx](file:///home/zl/work/workspaces/phowson/pages/Admin.tsx)（拆分/迁移或保留为兼容层）
- 复用组件：Toast/确认对话框、空状态、分页组件（放在 `components/` 下）

确认这个执行方案后，我会立刻：
1）创建分支 `refactor/admin-center`；
2）按 Epic A+B 的 P0 顺序提交第一批可运行的重构（路由+Layout+导航分组+守卫+统一组件基线），并附带对应测试用例。